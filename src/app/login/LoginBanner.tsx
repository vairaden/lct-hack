export default function LoginBanner({ className }: { className: string }) {
  return (
    <div
      className={className}
      style={{ height: "20rem", width: "15rem", backgroundColor: "red" }}
    ></div>
  );
}
